/**
 * DEPRECATED — the particle canvas belonged to the old minimalist design.
 * The neo-brutalist redesign replaced it with CSS textures (graph-paper,
 * halftone, scanlines). This stub exists only so stale imports can never
 * break the build. Safe to delete: `git rm components/ui/ParticleBackground.tsx`
 */
export default function ParticleBackground() {
  return null;
}
